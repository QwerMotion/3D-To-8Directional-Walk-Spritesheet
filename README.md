You need to have blender installed as this will use blender headless, specify everything that is statet on top of the script. 

Blockbench models work better when exported with "Export Groups as Armature" toggled off, then the auto zoom works. Otherwise the object will not fill the specified sprite, it can still
work, you will have to try a few times to get the right zoom number.
